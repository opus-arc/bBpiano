//
//  bBpianoCoreTests.swift
//  bBpianoCoreTests
//
//  Created by opus arc on 2026/4/2.
//

import XCTest

final class bBpianoCoreTests: XCTestCase {

    override func setUpWithError() throws {
        // Put setup code here. This method is called before the invocation of each test method in the class.
        // 在这里放初始化代码，每个测试方法执行前都会调用这个函数
        
    }

    override func tearDownWithError() throws {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
        // 在这里写清理代码，每个测试执行完后都会调用

    }

    func testExample() throws {
        // This is an example of a functional test case.
        // 这是一个功能测试示例
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        // 使用 XCTAssert 等函数来验证结果是否正确
        // Any test you write for XCTest can be annotated as throws and async.
        // 你的测试函数可以标记为 throws 或 async
        // Mark your test throws to produce an unexpected failure when your test encounters an uncaught error.
        // 如果函数标记为 throws，当出现未捕获错误时，测试会失败
        // Mark your test async to allow awaiting for asynchronous code to complete. Check the results with assertions afterwards.
        // 如果标记为 async，你可以等待异步代码执行完再断言结果
        
        
    }

    func testPerformanceExample() throws {
        // This is an example of a performance test case.
        // 性能测试示例
        measure {
            // Put the code you want to measure the time of here.
            // 把你想测试耗时的代码放在这里
        }
    }

}
